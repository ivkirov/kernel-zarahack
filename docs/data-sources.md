# Data Sources & Method — moved

This page has been split into two more detailed docs:

- **[datasets.md](datasets.md)** — the OSM / NSI / GeoNames inputs, why a fusion was needed,
  the province crosswalk, key assumptions, and the current data snapshot.
- **[data-pipeline.md](data-pipeline.md)** — the `data-engine` ETL steps and the PostgreSQL
  schema they produce.

See the [documentation index](../README.md#documentation) for the full set.
