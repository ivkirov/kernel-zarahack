# --- OpenKBS playground (existing) ---
node_modules/
.*
!.gitignore
!.env.example
!.github
!.github/**
!data-engine/.python-version

# --- Time Poverty Matrix ---
# Large / binary raw data — regenerate locally, never commit
data-engine/raw/*.pbf
data-engine/raw/*.xlsx
data-engine/out/*.csv
# ...except the seed CSVs the deployment loads via 03_seed_postgres.py
!data-engine/out/infrastructure_nodes.csv
!data-engine/out/demographic_weights.csv

# Secrets
.env

# Java
backend-api/target/

# Python
**/venv/
**/__pycache__/
*.pyc

# Node / Tailwind
frontend/node_modules/
frontend/dist/output.css

#Datasets
datasets/

# --- Deploy artifacts (scripts/deploy.sh) ---
# Stamps written per-deploy on the VM; the live commit, not source.
/deploy-stamp.json
/frontend/version.json
# Detached service PID files + logs.
/.run/
